#pragma once

#include <map>
#include <cmath>
#include <string>
#include <vector>
#include <random>
#include <iostream>

void show_board(std::vector<std::vector<int>>& board);
void make_board(std::vector<std::vector<int>>& board);